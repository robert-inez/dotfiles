# This file is no longer the active config.
# Shell aliases, init hooks (starship, direnv, zoxide), session variables,
# and PATH are now declared in home.nix via programs.fish.
#
# home-manager writes the active config.fish to ~/.config/fish/config.fish.
# This file is kept for reference only and is not symlinked.
